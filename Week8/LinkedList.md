Implementing a complete LinkedList class. Brief explanation:
A linked list is a chain of nodes. Each node holds data and a reference to the next node.
Unlike arrays, elements aren't stored contiguously; they're connected via references.
Operations: add at front/end, remove, search, traverse.